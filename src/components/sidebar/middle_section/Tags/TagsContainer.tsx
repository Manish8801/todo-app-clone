import TagsButtons from "./TagsButtons";
import TagsList from "./TagsList";

const TagsContainer = () => {
  return (
    <div>
      <TagsButtons />
      <TagsList/>
    </div>
  );
};

export default TagsContainer;
